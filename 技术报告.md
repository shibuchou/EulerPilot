# EulerPilot 技术报告

更新时间：`2026-07-27`

## 摘要

EulerPilot 是一个面向 openEuler 的自适应资源管控 Agent。项目通过 eBPF 进行低开销观测，在用户态完成 workload 分类、压力识别和策略决策，并通过 `cgroup v2` 与 `sched_ext/scx` 双执行后端实现资源调控。项目已经进入最终交付收口阶段，当前正式结论以同一 `tested_code_commit` 与 formal `artifact_id` 绑定后的 RUNS=10 套件为准。

EulerPilot 不追求无条件替代 Linux 默认调度器，而是面向混部干扰场景，通过 eBPF/PSI 感知 workload 状态，在延迟敏感服务与后台干扰共存时，选择 cgroup v2 或 sched_ext/scx 后端进行按需控制，并通过 Redis/Nginx 对照实验展示收益、边界与可回滚能力。

## 1. 项目背景

在服务器环境中，性能问题往往不是由单一 CPU 利用率指标决定的。当系统同时存在延迟敏感服务与后台干扰 workload 时，如何在保证可解释、可复现和可回滚的前提下进行资源调控，是一个典型的系统创新问题。

本赛题要求作品能够在 openEuler 环境中真实运行、形成正式实验结果、并完成完整报告。因此，EulerPilot 的设计目标不是给出一个最小调度器 demo，而是形成一套可交付的 Agent 框架：

```text
观测
-> 分析
-> 决策
-> 执行
-> 实验验证
-> 证据交付
```

## 2. 总体方案

EulerPilot 当前采用如下主链路：

```text
Kernel Observation
-> Observer Reader
-> Workload Analyzer
-> PsiGate
-> Policy Engine
-> Skill Manager
-> Resource / Scheduling / Network / Security Skills
-> Audit / Evidence / Rollback
```

其中：

- Kernel Observation 采集 eBPF sched tracepoints、PSI、cgroup 状态与事件。
- Workload Analyzer 识别 latency-sensitive、throughput batch、background noisy 等任务。
- PsiGate 判断压力状态，支持 `NORMAL -> ARMED -> ACTIVE -> COOLDOWN -> NORMAL`。
- Policy Engine 根据安全事件、压力状态和目标对象生成决策。
- Skill Manager 编排 Resource Control、Scheduling、Network、Security 等 Skill。
- AuditBus 与 ActionJournal 记录事件、动作、补偿和 rollback 结果。

## 3. 核心创新点

### 3.1 统一 Agent Framework

EulerPilot 没有把 cgroup v2、sched_ext、Network、Security 做成割裂 demo，而是通过统一 Runtime、Skill Registry、Skill Manager、Policy Engine 和 ActionJournal 管理多类系统能力。

### 3.2 双后端执行路径

项目同时支持：

- `cgroup v2`：官方发行内核上的稳定主后端。
- `sched_ext/scx`：在 SP4 官方源码自编译启用内核上完成验证的增强调度后端。

该设计保证了 SP3 强制兼容环境可交付，同时保留比赛核心的 sched_ext/scx 增强路线。

### 3.3 跨 Skill 联动

Policy Engine 可以将 Security anomaly 与 Resource/Network 动作串成事务链路。例如：

```text
security_policy burst_connect anomaly
  -> policy_engine decision
  -> resource_control cgroup 降级
  -> network_qos lab netdev 限速
  -> rollback
```

同一个 `transaction_id` 可以串起 security、policy_engine、resource_control、network_qos 和 ActionJournal。

### 3.4 Evidence-first 交付

项目通过 evidence manifest、compact report、quality gate、Web Console 和演示脚本，将实验和演示材料统一到可复现证据链中。旧结果不会被删除，而是通过 `evidence/evidence_status_overrides.json` 标记为 historical、provisional 或 invalid，避免误用。

## 4. 实验环境

| 环境 | 系统 / 内核 | 角色 |
|---|---|---|
| SP4 / `192.168.1.123` | openEuler 24.03 LTS SP4，自编译启用 `CONFIG_SCHED_CLASS_EXT` 的 `eulerpilot-scx` 内核 | 主开发、主验证、性能实验、Web、release candidate |
| SP3 / `192.168.1.121` | openEuler 24.03 LTS SP3 官方内核 | 比赛要求的强制兼容交付环境 |
| `192.168.1.122` | 历史 OLK/sched_ext 环境 | OLK/sched_ext 对照，不作为最终 release 来源 |

SP4 发行环境已完成适配验证；sched_ext/scx 路径基于 SP4 官方源码自编译启用内核完成复核，不声称发行默认内核直接支持 sched_ext。cgroup v2 是官方发行内核上的稳定执行后端。

## 5. 正式验证绑定

当前 formal artifact 结果绑定：

```text
tested_code_commit = 7a99d87048f4f2040377354bfe0ce21401664642
artifact_id = ef1baebec7ac138acd0eb1a59fc3880ca550330ef89f87615e8579a0ef264240
formal_experiment_root = /root/eulerpilot-runs/7a99d87048f4f2040377354bfe0ce21401664642/formal-experiments/20260726-162050
```

该绑定保证正式实验与源码、构建产物、配置和 benchmark 脚本一致，避免“实验运行的是旧二进制但报告引用新源码”的问题。

## 6. Redis 实验结果

正式 Redis RUNS=10 结果目录：

```text
/root/eulerpilot-runs/7a99d87048f4f2040377354bfe0ce21401664642/formal-experiments/20260726-162050/redis-scx-compare-runs10
```

基线：修正后的 `noisy_default`，前后台 workload 使用默认 cgroup 权重，不再使用旧的受控 baseline。

主要结论：

| 命令 | 模式 | RPS 平均变化 | 95% CI | P99 改善 | 95% CI |
|---|---|---:|---:|---:|---:|
| GET | noisy_cgroup_v2 | +68.30% | [50.74%, 85.86%] | +27.23% | [-28.22%, 82.67%] |
| INCR | noisy_cgroup_v2 | +83.09% | [65.21%, 100.98%] | +52.27% | [26.41%, 78.13%] |
| PING_INLINE | noisy_cgroup_v2 | +2.12% | [-4.34%, 8.59%] | -3.56% | [-19.60%, 12.48%] |
| SET | noisy_cgroup_v2 | -0.49% | [-3.30%, 2.31%] | -0.38% | [-14.79%, 14.03%] |

解释：

- Redis GET/INCR 在受干扰场景中有明确正向收益。
- PING/SET 结果接近中性或区间跨零，不作为强正向结论。
- SCX PSI/always-active 组合在部分 Redis 场景中存在回退，应作为策略边界如实说明。

## 7. Nginx 实验结果

正式 Nginx RUNS=10 结果目录：

```text
/root/eulerpilot-runs/7a99d87048f4f2040377354bfe0ce21401664642/formal-experiments/20260726-162050/nginx-scx-compare-runs10
```

主要结论：

| 模式 | RPS 平均变化 | 95% CI | P99 改善 | 95% CI |
|---|---:|---:|---:|---:|
| noisy_cgroup_v2 | +7.07% | [4.51%, 9.63%] | +18.49% | [13.87%, 23.11%] |
| noisy_scx_normal | -0.13% | [-1.81%, 1.55%] | -11.86% | [-18.87%, -4.86%] |
| noisy_scx_always_active | -19.76% | [-24.75%, -14.78%] | -68.93% | [-99.97%, -37.89%] |
| noisy_scx_psi | -27.09% | [-28.71%, -25.46%] | -70.39% | [-85.60%, -55.17%] |

解释：

- Nginx 场景证明 EulerPilot 可以迁移到第二业务线。
- cgroup v2 在本次 Nginx noisy 场景中呈现 RPS 与 P99 改善。
- 当前 scx 策略对短连接、高频唤醒型 workload 存在适配边界。
- 项目不把 Nginx 结果包装成“sched_ext 全面提升”，而是作为 workload-sensitive 调优边界保留。

## 8. Throughput-first 验证

正式结果目录：

```text
/root/eulerpilot-runs/7a99d87048f4f2040377354bfe0ce21401664642/formal-experiments/20260726-162050/throughput-first-runs10
```

本轮修复后，Throughput validity 拆分为：

- `collection_valid`
- `classification_valid`
- `dispatch_accounting_valid`
- `workload_completion_valid`
- `scheduler_stability_valid`
- `performance_result`

当前 formal artifact 结果显示 throughput-first 10 轮 validity=pass，并使用本轮 counter delta 和 class-aware dispatch accounting，解决了旧结果中 `enqueue_batch>0` 但无 batch dispatch 证据仍被标 pass 的问题。

## 9. Mixed-Adaptive 验证

正式结果目录：

```text
/root/eulerpilot-runs/7a99d87048f4f2040377354bfe0ce21401664642/formal-experiments/20260726-162050/mixed-adaptive-runs10
```

验证目标：

```text
quiet
-> pressure
-> recovery
```

同一 Agent instance 必须在连续 trace 中按序出现：

```text
NORMAL -> ARMED -> ACTIVE -> COOLDOWN -> NORMAL
```

当前 formal artifact 结果显示 mixed-adaptive 10 轮 validity=pass。该实验定位为功能闭环验证，不作为净性能提升结论。

## 10. Agent 开销

正式结果目录：

```text
/root/eulerpilot-runs/7a99d87048f4f2040377354bfe0ce21401664642/formal-experiments/20260726-162050/agent-overhead-runs10
```

结果摘要：

| 模式 | 平均 CPU 占用 | RSS 平均 |
|---|---:|---:|
| observe_only_cgroup | 约 0.45% 单核 | 约 7.5 MB |
| active_cgroup | 约 0.51% 单核 | 约 7.4 MB |
| active_sched_ext | 约 0.55% 单核 | 约 7.5 MB |

说明：用户态 Agent 控制面开销较低，适合作为系统旁路控制进程运行。

## 11. Kubernetes 与真实 Pod 验证

EulerPilot 已在 k3s 真实 Pod 环境完成旁路、隔离、可清理验证：

- target resolver
- real Pod resource control
- Pod host veth QoS
- Pod host veth XDP
- real Pod Policy Engine cross-skill 联动

验证使用独立 namespace、独立 label、有限 CPU/Memory requests 和 limits，不修改生产 namespace 或现有 workload；验证完成后通过 label selector 复查无残留。

## 12. 质量门禁

当前 final gate：

```text
SP4 final gate: 29/29 P0 + 100 smoke + 5 doctor-safe
SP3 compatibility final gate: 10/10
```

主要日志：

- `reports/final_quality_gate_20260726-v6-sp4-final.log`
- `reports/final_quality_gate_20260726-v6-sp3-final.log`
- `reports/gates/sp3_capability_matrix_20260726-v6-final.json`

Evidence release gate：

```bash
python3 scripts/collect_final_evidence.py --validate-release
```

当前结果：

```text
entries=42
missing_required=0
warnings=0
```

## 13. 最终证据入口

| 类型 | 路径 |
|---|---|
| evidence manifest | `configs/final_evidence_manifest.json` |
| evidence collector | `scripts/collect_final_evidence.py` |
| compact markdown | `reports/final_evidence_compact.md` |
| compact json | `reports/final_evidence_compact.json` |
| evidence index | `docs/final_evidence_index.md` |
| final results summary | `docs/final_results_summary.md` |
| demo runbook | `docs/demo_final_runbook.md` |
| user manual | `用户手册.md` |
| demo video | `项目演示视频.mp4` |

## 14. 结论与边界

EulerPilot 当前已证明：

1. 统一 Agent Framework 可以支撑观测、分类、决策、执行、回滚和证据链。
2. cgroup v2 在官方发行内核上是稳定资源治理主路径。
3. Redis GET/INCR 受干扰场景中，cgroup v2 动态治理有明确收益。
4. Nginx 场景证明项目可迁移到第二业务线，但需要按 workload 边界解释收益。
5. sched_ext/scx 控制链、batch dispatch 记账和 mixed-adaptive 状态闭环可运行，但性能收益必须按 workload 区分，不能宣传为全面稳定提升。
6. Resource、Network、Security 和 Policy Engine 已通过统一 Skill Framework 形成可扩展 OS Agent 能力。
7. Web Console、用户手册、演示视频、evidence compact 和质量门禁共同支撑最终交付。

项目最终表达应保持克制：

```text
EulerPilot 的价值不在于声称所有 workload 永远优于默认调度器，
而在于可观测、可决策、可执行、可回滚、可审计和可复现。
```

