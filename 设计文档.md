# EulerPilot 设计文档

更新时间：`2026-07-27`

## 1. 项目定位

EulerPilot 是一个面向 openEuler 的自适应资源管控 Agent。项目围绕“观测 -> 决策 -> 执行 -> 反馈 -> 证据”构建闭环，通过 eBPF、PSI、cgroup v2、sched_ext/scx、TC/XDP 和 BPF LSM 等系统能力，实现对不同 workload 的识别、资源治理、安全审计、网络策略和跨 Skill 联动。

EulerPilot 不是单一调度器 demo，而是一套统一的 OS Agent Framework。它在用户态运行 Agent Runtime、Workload Analyzer、PsiGate、Policy Engine 和 Skill Manager，在内核侧通过 eBPF 观测 workload 行为，并通过多个 Skill 执行资源、调度、网络和安全策略。

## 2. 设计目标

项目的核心设计目标如下：

1. 在 openEuler 环境中形成真实可运行、可测试、可演示的系统级 Agent。
2. 通过低开销观测识别 latency-sensitive、throughput batch、background noisy 等 workload。
3. 在官方发行内核上使用 cgroup v2 作为稳定资源治理主路径。
4. 在 SP4 官方源码自编译启用 `CONFIG_SCHED_CLASS_EXT` 的内核上验证 sched_ext/scx 增强调度路径。
5. 通过统一 Skill Framework 扩展 Resource、Network、Security 和 Policy Engine 能力。
6. 对所有有副作用的动作提供审计、事务记录、验证和 rollback。
7. 以 evidence manifest、质量门禁、Web Console 和演示脚本形成可复现交付链。

## 3. 总体架构

![EulerPilot 主架构图](docs/assets/architecture/eulerpilot_main_architecture.svg)

主运行闭环：

```text
Workload
-> Kernel Observation
-> Runtime / Analyzer
-> PsiGate / Policy Engine
-> Skill Manager
-> Kernel Execution
-> Workload
-> Audit / Evidence / Feedback
```

实线箭头表示运行时数据流、决策流和执行反馈；虚线箭头表示能力检测、安全约束、质量门禁和环境验证关系。

`cgroup v2` 是当前稳定资源治理主路径；`sched_ext/scx` 是比赛核心增强调度路径；Network、Security 和 Resource Control 均通过统一 Skill Framework 管理。

## 4. 环境与交付矩阵

| 环境 | 定位 | 验证内容 |
|---|---|---|
| SP4 / `192.168.1.123:/root/EulerPilot` | 主开发、主验证和性能实验环境 | sched_ext/scx、Web Console、完整集成、formal artifact、RUNS=10 实验、最终 evidence |
| SP3 / `192.168.1.121:/root/EulerPilot` | 比赛要求的强制兼容环境 | 构建、配置校验、cgroup v2 主闭环、安全扩展 smoke、rollback、safe doctor、sched_ext graceful fallback |
| `192.168.1.122` | 历史 OLK/sched_ext 对照 | OLK-6.6 与早期 sched_ext/scx 对照验证，不作为最终 release 来源 |

统一表述：

```text
SP4 发行环境已完成适配验证；
sched_ext/scx 基于 SP4 官方源码自编译启用 CONFIG_SCHED_CLASS_EXT 的内核完成复核；
不声称 SP4 发行默认内核直接支持 sched_ext。
```

## 5. 模块边界

| 目录 | 职责 |
|---|---|
| `agent/` | 用户态控制核心，负责配置解析、生命周期、状态聚合、workload 分类、target 解析、Policy Engine 和 Skill 管理 |
| `bpf/` | 低开销内核观测和 hook 程序，包括 workload observer、network connect4/TC/XDP、security LSM/syscall tracing |
| `sched/` | sched_ext/scx 调度器、BPF map、DSQ 分流和 `scx_eulerpilot` 构建脚本 |
| `configs/` | 默认配置、Skill 配置、Policy Engine 联动配置和 final evidence manifest |
| `bench/` | Redis、Nginx、throughput、mixed-adaptive、Agent overhead 等可复现实验入口 |
| `tests/` | 单元测试、集成测试、benchmark 验证和真实 runtime/Pod 验证 |
| `web_console/` | 旁路展示控制台，读取现有证据和白名单脚本，不进入 Agent 热路径 |
| `reports/` / `results/` | 质量门禁、事件日志、图表、实验报告和历史结果 |
| `docs/` | 设计、报告、用户手册、答辩和最终交付材料 |

## 6. Agent 控制面设计

Agent Runtime 提供统一生命周期：

```text
load config
-> detect capabilities
-> start enabled skills
-> collect observer / PSI / skill events
-> build snapshots
-> make decisions
-> apply actions through allowed skills
-> audit and journal
-> stop_all rollback
```

核心公共组件：

| 组件 | 说明 |
|---|---|
| CapabilityDetector | 探测 BTF、BPF LSM、XDP、TC、cgroup v2、PSI、sched_ext、Kubernetes 等能力 |
| TargetResolver | 将 PID、cgroup、container、container_id、Kubernetes Pod 解析为真实 cgroup 或 host veth |
| Workload Analyzer | 根据 comm、eBPF 指标和运行时特征识别 workload 类型 |
| PsiGate | 管理 `NORMAL -> ARMED -> ACTIVE -> COOLDOWN -> NORMAL` 压力状态机 |
| Policy Engine | 根据事件和策略生成跨 Skill decision、transaction 和 rollback 动作 |
| Skill Manager | 统一管理 Skill 注册、启停、doctor、snapshot、rollback |
| AuditBus | 统一输出 decision、applied、restored、failed 等事件 |
| ActionJournal | 持久化动作和补偿状态，支持失败恢复和证据追溯 |

## 7. 执行后端

```text
Policy Engine / Skill Manager
  -> CgroupExecutor   稳定主路径
  -> ScxExecutor      sched_ext 环境确认后的增强路径
  -> NetworkExecutor  TC / XDP / connect4
  -> SecurityExecutor BPF LSM / syscall tracing
```

### 7.1 CgroupExecutor

CgroupExecutor 是官方发行内核上的稳定执行后端，支持：

- `cpu.weight`
- `cpu.max`
- `memory.high / memory.low / memory.max`
- `io.weight / io.max`
- 旧值读取、写入验证、复读校验和 rollback

当前封版默认关闭动态 cpuset topology，稳定资源控制主路径是 `cpu.weight/cpu.max + memory + io`。

### 7.2 ScxExecutor

ScxExecutor 是 sched_ext/scx 增强调度后端，在 SP4 自编译启用 `CONFIG_SCHED_CLASS_EXT` 的内核上复核。它通过 pinned BPF maps 将用户态分类结果传给 `scx_eulerpilot`：

- `class_map`
- `gate_state_map`
- `stats`

调度器按 latency、batch、background 等 workload class 分流，并通过 formal artifact 记录二进制、BPF 对象、构建参数和运行证据。

### 7.3 NetworkExecutor

Network 能力默认限制在 lab netdev 或已解析的安全 Pod host veth 上，支持：

- cgroup/connect4 network policy
- TC/TBF QoS
- XDP drop/pass
- isolated veth 与真实 Pod host veth 验证
- attach / verify / rollback / cleanup

### 7.4 SecurityExecutor

Security 能力支持：

- BPF LSM file/exec/socket/ptrace/capability scope 检查
- syscall tracing audit
- anomaly 聚合
- credential 生命周期事件
- scoped enforce 与 fail-closed 能力判定

封版配置中拒绝无 cgroup scope 的全局 enforce。

## 8. Skill 能力设计

| Skill | 主要能力 | 当前状态 |
|---|---|---|
| `resource_control` | CPU/Memory/IO、target_ref、container/Pod cgroup、quota sweep、rollback | 已完成 |
| `network_policy` | connect4、TC QoS、XDP、real Pod host veth、安全边界 | 已完成 |
| `security_policy` | BPF LSM、syscall tracing、anomaly、credential lifecycle、scope 过滤 | 已完成 |
| `policy_engine` | 跨 Skill 联动、统一 transaction、多动作失败回滚 | 已完成 |
| `psi_gate` | PSI 状态机、自适应 gate、mixed-adaptive 闭环 | 已完成 |
| `web_console` | Evidence-first 展示、白名单 Demo、SSE job 日志、单任务锁 | 已完成 |

## 9. Policy Engine 联动设计

当前已完成的核心联动链路：

```text
security_policy burst_execve
  -> policy_engine
  -> resource_control cgroup 降级
  -> rollback

security_policy burst_connect
  -> policy_engine
  -> resource_control demo_cgroup
  -> network_qos lab_netdev
  -> rollback

security_policy burst_connect
  -> policy_engine target_ref=lab_pod(type=k8s_pod)
  -> resource_control Pod cgroup
  -> network_qos Pod host veth
  -> rollback
```

联动事件通过统一字段串联：

```json
{
  "transaction_id": "pe-v3-1-xxxx",
  "trigger_event_id": "sec-xxxx",
  "policy_id": "security_network_resource_response",
  "stage": "decision|applied|restored|failed"
}
```

## 10. 安全与回滚设计

EulerPilot 对所有高风险能力采用默认关闭或严格边界：

- 外部业务 cgroup 默认严格只读。
- Network XDP 管理网卡危险模式默认关闭。
- TC 修改已有非自有 qdisc 默认关闭。
- `memory.reclaim` 标记为不可逆动作，默认关闭。
- 无法确认 ownership 的 rollback 不执行破坏性清理。
- `doctor-safe` 严格只读，不加载 BPF/TC/XDP/LSM，不创建 cgroup。
- Web Console 只允许 `actions.yaml` 白名单动作，并使用单任务锁、SSE 日志和 cleanup。

rollback 状态至少区分：

```text
ROLLED_BACK
PARTIAL_ROLLBACK
ROLLBACK_FAILED
NOT_REVERSIBLE
```

## 11. Web Console 设计

Web Console 是比赛展示和受控演示入口，定位为：

```text
Evidence-first + 白名单 Demo + 旁路展示控制台
```

它不进入 Agent 热路径，不产生新的性能结论，只负责展示现有 CLI、测试脚本、事件日志、Prometheus metrics 和 evidence 文件。

主要页面：

- Overview
- Skills & Agent
- Scheduling & PSI
- eBPF Extensions
- Policy Engine Timeline
- Evidence & Live Demo

## 12. 证据和交付设计

最终证据链由以下入口收口：

- `configs/final_evidence_manifest.json`
- `scripts/collect_final_evidence.py`
- `reports/final_evidence_compact.md`
- `reports/final_evidence_compact.json`
- `docs/final_evidence_index.md`
- `docs/final_report_submission.md`
- `docs/demo_final_runbook.md`

当前 evidence compact 覆盖 `42` 条核心证据，必需缺失 `0`、warning `0`。

## 13. 设计边界

- 不声称 SP4 发行默认内核直接支持 sched_ext。
- 不声称所有 workload 都稳定优于默认调度器。
- 不把旧 historical/provisional 结果作为 final positive evidence。
- 不把 Web Console 作为实验数据源。
- Kubernetes 验证必须旁路、隔离、可清理。
- `third_party/reference/` 仅作为参考快照，不作为 EulerPilot 生产代码直接复制。

