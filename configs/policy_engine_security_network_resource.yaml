agent:
  name: EulerPilot
  mode: dry-run
  interval_ms: 1000
skills_config_path: policy_engine_security_network_resource.skills.yaml

scheduler:
  type: cgroup_v2

exporter:
  prometheus:
    enabled: false
    listen: 127.0.0.1:9108
