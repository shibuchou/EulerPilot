agent:
  name: EulerPilot
  mode: dry-run
  interval_ms: 1000
skills_config_path: skills.yaml

scheduler:
  type: cgroup_v2
  # Optional sched_ext override. Resolution order is YAML > EULERPILOT_SCX_BINARY > PATH > /usr/local/bin.
  # binary: /usr/local/bin/scx_eulerpilot

exporter:
  prometheus:
    enabled: false
    listen:  127.0.0.1:9108
