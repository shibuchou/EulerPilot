daemon off;
worker_processes 1;
error_log /root/EulerPilot/results/final/nginx-scx-compare-20260706-101928/error.log;
pid /root/EulerPilot/results/final/nginx-scx-compare-20260706-101928/nginx.pid;

events { worker_connections 1024; }

http {
    server {
        listen 18082;
        location / {
            return 200 'EulerPilot nginx sched_ext compare\n';
        }
    }
}
