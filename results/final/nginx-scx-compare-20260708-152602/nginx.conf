daemon off;
worker_processes 1;
error_log /root/EulerPilot/results/final/nginx-scx-compare-20260708-152602/error.log;
pid /root/EulerPilot/results/final/nginx-scx-compare-20260708-152602/nginx.pid;

events { worker_connections 1024; }

http {
    server {
        listen 18082;
        location / {
            return 200 'EulerPilot nginx sched_ext compare\n';
        }
    }
}
