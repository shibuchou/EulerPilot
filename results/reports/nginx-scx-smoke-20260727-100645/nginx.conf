daemon off;
worker_processes 1;
error_log /root/EulerPilot/results/reports/nginx-scx-smoke-20260727-100645/error.log;
pid /root/EulerPilot/results/reports/nginx-scx-smoke-20260727-100645/nginx.pid;

events { worker_connections 1024; }

http {
    server {
        listen 18081;
        location / {
            return 200 'EulerPilot nginx sched_ext smoke\n';
        }
    }
}
