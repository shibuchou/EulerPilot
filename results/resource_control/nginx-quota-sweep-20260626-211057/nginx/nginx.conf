daemon off;
worker_processes 1;
error_log /root/EulerPilot/results/resource_control/nginx-quota-sweep-20260626-211057/nginx/error.log;
pid /root/EulerPilot/results/resource_control/nginx-quota-sweep-20260626-211057/nginx/nginx.pid;

events {
    worker_connections 2048;
}

http {
    access_log off;
    server {
        listen 18083;
        location / {
            return 200 'EulerPilot nginx resource-control quota sweep\n';
        }
    }
}
